{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "WorkerEvent",
  "type": "object",
  "required": ["id", "stream_id", "type", "payload", "timestamp"],
  "additionalProperties": false,
  "$defs": {
    "discordChatPayload": {
      "type": "object",
      "description": "Discord chat overlay payload using the canonical v2 author_id and content fields.",
      "additionalProperties": true,
      "required": ["author_id", "content"],
      "not": {
        "anyOf": [
          { "required": ["user_id"] },
          { "required": ["text"] }
        ]
      },
      "properties": {
        "message_id": { "type": "string" },
        "author_id": {
          "type": "string",
          "description": "Canonical Discord author ID."
        },
        "display_name": { "type": "string" },
        "content": {
          "type": "string",
          "description": "Canonical Discord message content."
        },
        "avatar_url": { "type": "string" },
        "is_bot": { "type": "boolean" },
        "text_channel_id": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" }
      }
    }
  },
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "stream_id": { "type": "string", "minLength": 1 },
    "service_id": { "type": "string", "minLength": 1 },
    "type": {
      "type": "string",
      "pattern": "^(overlay\\.|caption\\.)",
      "examples": [
        "overlay.current_time",
        "overlay.participants",
        "overlay.active_speaker",
        "overlay.discord_chat",
        "caption.telop",
        "caption.final"
      ]
    },
    "payload": {
      "type": "object",
      "description": "Event payload. overlay.discord_chat carries canonical author_id and content plus message_id, display_name, avatar_url, is_bot, text_channel_id, and created_at.",
      "additionalProperties": true
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "type": { "const": "overlay.discord_chat" }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "payload": { "$ref": "#/$defs/discordChatPayload" }
        }
      }
    }
  ]
}
