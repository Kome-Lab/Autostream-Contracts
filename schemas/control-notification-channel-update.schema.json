{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.autostream.example.com/control-notification-channel-update.schema.json",
  "title": "ControlNotificationChannelUpdateRequest",
  "description": "Control Panel notification-channel update. Per-channel SMTP fields and uses_global_smtp are intentionally not accepted. Omitting email_recipients preserves the existing masked recipient set, and the backend preserves the channel's existing legacy/global delivery mode.",
  "type": "object",
  "required": ["name", "type", "enabled"],
  "additionalProperties": false,
  "properties": {
    "name": { "type": "string", "minLength": 1 },
    "type": { "enum": ["discord", "slack", "generic", "email"] },
    "enabled": { "type": "boolean" },
    "webhook_url": {
      "type": "string",
      "format": "uri",
      "pattern": "^https?://",
      "writeOnly": true,
      "description": "Optional write-only replacement for a Discord, Slack, or generic webhook URL. Omission preserves the existing encrypted URL; it is never returned raw."
    },
    "email_recipients": {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "format": "email",
        "maxLength": 320,
        "pattern": "^[^\\r\\n]+$",
        "description": "Bare mailbox address only; display names and CR/LF are rejected."
      },
      "description": "Optional replacement recipient set. Omission preserves existing recipients; an explicitly empty array is invalid."
    },
    "severity_filter": {
      "type": "array",
      "items": { "enum": ["info", "warning", "error", "critical"] },
      "uniqueItems": true
    },
    "event_type_filter": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true
    }
  }
}
