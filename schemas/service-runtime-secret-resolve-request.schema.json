{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.autostream.example.com/service-runtime-secret-resolve-request.schema.json",
  "title": "Service Runtime Secret Resolve Request",
  "type": "object",
  "additionalProperties": false,
  "required": ["service_id", "secret_name"],
  "properties": {
    "service_id": {
      "type": "string",
      "minLength": 1
    },
    "stream_id": {
      "type": "string",
      "description": "Required when resolving stream-scoped integration secrets such as Drive destination folder IDs."
    },
    "archive_profile_id": {
      "type": "string",
      "description": "Required when resolving archive runtime secret references sent in encoder start payloads."
    },
    "secret_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Secret reference name previously returned in the authenticated service runtime config. Resolving the same service/context/secret while its runtime lease is active returns runtime_secret_lease_active."
    }
  }
}
