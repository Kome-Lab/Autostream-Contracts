{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.autostream.example.com/oauth-user-link-write.schema.json",
  "title": "OAuthUserLinkWriteRequest",
  "type": "object",
  "required": ["provider_id", "subject"],
  "additionalProperties": false,
  "properties": {
    "provider_id": { "type": "string" },
    "provider_type": { "type": "string", "enum": ["google", "github", "discord"] },
    "subject": {
      "type": "string",
      "description": "Legacy provider subject field. Production Control Panel rejects manual link creation and creates links only through the OAuth callback ceremony."
    },
    "email": { "type": "string" }
  },
  "description": "Legacy administrative shape retained for compatibility. Production links must be created through the OAuth callback ceremony; manual subject entry is disabled."
}
