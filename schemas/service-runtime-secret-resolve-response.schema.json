{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.autostream.example.com/service-runtime-secret-resolve-response.schema.json",
  "title": "Service Runtime Secret Resolve Response",
  "type": "object",
  "additionalProperties": false,
  "required": ["secret_name", "value", "expires_in_sec"],
  "properties": {
    "secret_name": {
      "type": "string"
    },
    "value": {
      "type": "string",
      "description": "Raw runtime secret returned only to the authenticated service. Do not expose it to UI, audit metadata, diagnostics, or logs."
    },
    "expires_in_sec": {
      "type": "integer",
      "minimum": 1,
      "description": "Lease lifetime for this resolved value. During this window, the same service/context/secret returns runtime_secret_lease_active instead of the raw value."
    }
  }
}
